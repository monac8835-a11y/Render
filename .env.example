# This server needs no secrets - it only relays WebRTC handshake data.
# Hosting platforms set PORT automatically; you don't need to set it
# yourself. This file exists only so the project follows the same
# ".env.example, never commit real .env" convention as the main app.
PORT=8080
